# google-doc-to-database
## To run this
You will need to supply your google token and google api creds in `token.json` and `client_secreet.json` files.
After that you just need to run the `main.py` file and you're good to go!
